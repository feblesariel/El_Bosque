    <!-- ##### Right Side Cart Area ##### -->
    <div class="cart-bg-overlay"></div>

    <div class="right-side-cart-area">

        <% if (cart !== undefined) { %>

        <!-- Cart Button -->
        <div class="cart-button">
            <a href="#" id="rightSideCart"><img src="/img/core-img/bag.svg" alt=""> <span><%= cart ? Object.keys(cart).length : 0 %></span></a>
        </div>

        <div class="cart-content d-flex">
            <!-- Cart List Area -->
            <div class="cart-list">

                <% for (const key in cart) { %>
                    <% const item = cart[key]; %>

                <!-- Single Cart Item -->
                <div class="single-cart-item">
                    <a href="/products/detail/<%=item.product_id%>" class="product-image">
                        <img src="/img/product-img/<%=item.image%>" class="cart-thumb" alt="">
                        <!-- Cart Item Desc -->
                        <div class="cart-item-desc">
                            <span class="product-remove"><i class="fa fa-close" aria-hidden="true"></i></span>
                            <span class="badge"><%=item.category%></span>
                            <h6><%=item.name%></h6>
                            <p class="size"><%=item.selectedOptions%></p>
                            <p class="price">$<%=item.price%></p>
                        </div>
                    </a>
                </div>               
                
                <% } %>

            </div>

            <!-- Cart Summary -->
            <div class="cart-amount-summary">

                <h2>Summary</h2>
                <ul class="summary-table">
                    <li><span>subtotal:</span> <span>$274.00</span></li>
                    <li><span>delivery:</span> <span>Free</span></li>
                    <li><span>discount:</span> <span>-15%</span></li>
                    <li><span>total:</span> <span>$232.00</span></li>
                </ul>
                <div class="checkout-btn mt-100">
                    <a href="checkout.html" class="btn essence-btn">check out</a>
                </div>
            </div>
        </div>

        <% } %>

    </div>
    <!-- ##### Right Side Cart End ##### -->