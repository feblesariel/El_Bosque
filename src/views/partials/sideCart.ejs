    <!-- ##### Right Side Cart Area ##### -->
    <div class="cart-bg-overlay"></div>

    <div class="right-side-cart-area">

        <% if (cart !== undefined) { let total = 0 %>

        <!-- Cart Button -->
        <div class="cart-button">
            <a href="#" id="rightSideCart"><img src="/img/core-img/bag.svg" alt=""> <span><%= cart ? Object.keys(cart).length : 0 %></span></a>
        </div>

        <div class="cart-content d-flex">
            <!-- Cart List Area -->
            <div class="cart-list">

                <% for (const key in cart) { %>
                    <% const item = cart[key]; %>
                    <% total += (parseFloat(item.price) * parseFloat(item.quantity)) %>

                <!-- Single Cart Item -->
                <div class="single-cart-item">
                    <div class="product-image">
                        <img src="/img/product-img/<%=item.image%>" class="cart-thumb" alt="">
                        <!-- Cart Item Desc -->
                        <div class="cart-item-desc">
                            <a href="/cart/remove/<%=key%>"><span class="product-remove"><i class="fa fa-close" aria-hidden="true"></i></span></a>
                            <span class="badge"><%=item.category%></span>
                            <h6><%=item.name%></h6>
                            <p class="price">$<%=item.price%></p>
                        </div>
                    </div>
                </div>               
                
                <% } %>

            </div>

            <!-- Cart Summary -->
            <div class="cart-amount-summary">

                <h2>Resumen</h2>
                <ul class="summary-table">
                    <li><span>items:</span> <span><%= cart ? Object.keys(cart).length : 0 %></span></li>
                    <li><span>total:</span> <span>$<%=total.toFixed(2)%></span></li>
                </ul>
                <span>¡Al realizar tu pedido recibis un cupón del 10% de descuento para tu próxima compra!</span>
                <div class="checkout-btn mt-100">
                    <a href="/checkout/" class="btn essence-btn">finalizar</a>
                </div>
            </div>
        </div>

        <% } else { %>

        <!-- Cart Button -->
        <div class="cart-button">
            <a href="#" id="rightSideCart"><img src="/img/core-img/bag.svg" alt=""> <span><%= cart ? Object.keys(cart).length : 0 %></span></a>
        </div>

        <div class="row justify-content-center align-items-center" style="height: 100vh;">
            <div class="col text-center">
                <span class="text-uppercase font-weight-bold">tu carrito esta vacio!</span>
                <div class="checkout-btn mt-100">
                    <a href="/products/shop/" class="btn essence-btn">comprar</a>
                </div>
            </div>
        </div>        

        <% } %>

    </div>
    <!-- ##### Right Side Cart End ##### -->